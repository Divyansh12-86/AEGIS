# EGPM core (PRD §30: pinned environment)
numpy>=2.0
scipy>=1.14
torch>=2.4
pytest>=8.0

# optional dataset loaders (PRD §3.1: N-CMAPSS .h5 / MIMII wav)
h5py>=3.11; platform_system != "Windows"
soundfile>=0.12
