# EGPM core (PRD §30: pinned environment)
numpy>=2.0
scipy>=1.14
torch>=2.4
pyyaml>=6.0
pytest>=8.0

# optional dataset loaders (PRD §3.1: N-CMAPSS .mat / MIMII wav)
h5py>=3.11; platform_system != "Windows"
soundfile>=0.12

# legacy AEGIS dashboard services (pre-EGPM boilerplate)
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
streamlit==1.38.0
plotly==5.24.0
python-dotenv==1.0.1
